# Core Backend Dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# AI/ML Dependencies
# Using cpu version of torch for smaller slug size if possible, 
# but simply listing it here uses standard. 
# Render free tier might struggle with large torch.
torch==2.1.1
transformers==4.35.2
openai-whisper
numpy==1.24.3

# Database
supabase==2.1.0

# Real-time Communication
websockets==12.0

# Utility
requests==2.31.0
python-dotenv==1.0.0